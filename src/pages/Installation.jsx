import React, { useEffect } from 'react';

const Installation = () => {
    useEffect (() => {
            document.title = "Installation | GAMEKEEPER";
        },[]);
    return (
        <div>
            installation
        </div>
    );
};

export default Installation;