import React, { useEffect } from 'react';

const Games = () => {
    useEffect (() => {
            document.title = "Games | GAMEKEEPER";
        },[]);
    return (
        <div>
            G
        </div>
    );
};

export default Games;